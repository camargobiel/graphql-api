type Query {
  characters: [Character]

  character(id: Int): Character
}

type Character {
  id: Int
  name: String
  gender: String
  status: String
  type: String
  origin: Origin
}

type Origin {
  name: String
  url: String
}